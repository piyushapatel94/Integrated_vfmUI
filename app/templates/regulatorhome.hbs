


{{#if programInitiate}}
    {{#modal-dialog close="toggleModal"
                  targetAttachment="center"
                  translucentOverlay=true}}
      <div class="loginmodal-container">
        <p>
          After Approving Invoice from Anchor ,You should Initiate the payment !!
          {{modalmessage}}
        </p>
 <input type="button" class="btn btn-primary pull-right" value="OK" {{action 'okgotohome'}}>
   </div>

    {{/modal-dialog}}

{{else if PORaised}}
    {{partial 'step8'}}
{{else if closeprogram}}
    {{partial 'step9' }}
{{else if  Invoiceapprove}}
    {{partial 'initaitepayment'}}
{{else}}
    <div class="row padding-10">
      <input type="button" class="btn btn-primary pull-right" value="Create New Program" {{action 'createprogram'}}>
    </div>
   {{!--
{{models-table data=model columns=columns close='closure' test='detailprogram'}}--}}

<table >
    <thead>
      <tr>
        <th>Program</th>
        <th>Date</th>
        <th>Status</th>
        
    <th>close</th>
      </tr>
    </thead>
    

     {{#each record as |item|}}
      <tr>
      <td style="padding:3px">  {{item.programid}}</td>
      <td style="padding:3px">  {{item.date}}</td>
      <td style="padding:3px"> <a  {{action 'detailprogram' item.status item.programid}}>{{item.status}} </a></td>
      <td  style="padding:3px">  <button {{action 'closeProgram' message item}}>Close</button></td>
      </tr>
      {{/each}}
     
   
    
  </table>
{{/if}}

{{#if approveProperty}}
  {{#modal-dialog close="toggleModal"
                  targetAttachment="center"
                  translucentOverlay=true}}
   <div class="loginmodal-container">
      <p>
        Program Close
      {{modalmessage}}
      </p>
 <input type="button" class="btn btn-primary pull-right" value="OK" {{action 'gotohome'}}>
   </div>

  {{/modal-dialog}}
{{/if}}
